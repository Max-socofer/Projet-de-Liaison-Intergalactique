name: Deploy to staging

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    environment: staging

    steps:
      - uses: actions/checkout@v4
      - run: echo "Déploiement STAGING pour Jira"
