name: Deploy to development

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    environment: development

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Fake deploy
        run: echo "Déploiement DEV pour Jira"
